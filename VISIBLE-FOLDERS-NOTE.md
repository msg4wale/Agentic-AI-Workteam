# Visible Agent and Skill Mirrors

The `agents/` and `skills/` folders are provided as visible mirrors because some ZIP/file browsers hide `.github`.

For installation in VS Code / GitHub Copilot, use the canonical folders:

- `.github/agents/`
- `.github/skills/`

The visible folders contain the same agent and skill definitions for inspection and easy copying.
